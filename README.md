# Excel 数据转换工具

一个可视化的 Excel 数据转换工具，支持智能字段映射和值转换规则配置。

## 功能特性

- 🎯 **智能字段映射** - 自动分析源数据和目标模板，建立字段映射关系
- 🔧 **手动调整映射** - 可视化拖拽连线，手动建立或修改映射关系
- 🔄 **值转换规则** - 配置字段值转换规则（如"已完税"→"超出免税部分"）
- 📊 **数据预览** - 查看源数据前5行，方便确认字段内容
- 💾 **配置保存** - 保存映射配置，下次使用直接加载
- 📈 **必填字段检查** - 自动检测缺失的必填字段
- 🎨 **可视化连线** - 清晰的映射连线，支持点击删除和设置转换规则

## 环境要求

- Node.js 14.0 或更高版本
- 支持的操作系统：Windows / macOS / Linux

## 安装步骤

### 1. 安装 Node.js

如果还没有安装 Node.js：

- **Windows**: 访问 https://nodejs.org/ 下载 LTS 版本安装
- **macOS**: 使用 Homebrew 安装 `brew install node`
- **Linux**: 使用包管理器安装，如 `sudo apt install nodejs npm`

### 2. 安装依赖

在项目目录下打开命令行，执行：

```bash
npm install
```

## 使用方法

### 方式一：使用启动脚本

```bash
# Windows
双击运行 start.bat

# macOS / Linux
./start.sh
```

### 方式二：命令行启动

```bash
node transform_excel.js
```

### 方式三：开发模式

```bash
node src/index.js
```

启动后，在浏览器中打开显示的地址（默认 http://localhost:3456），通过界面上传文件。

## 操作说明

1. 将源数据 Excel 文件拖拽到左侧区域（或点击选择文件）
2. 将目标模板 Excel 文件拖拽到右侧区域
3. 系统自动分析并建立字段映射，可手动调整映射关系
4. **手动映射**：先点击源字段蓝色圆点（变红），再点击目标字段圆点即可建立连线
5. **删除映射**：点击连线可删除该映射关系
6. **设置转换规则**：点击连线中间的⚙按钮可配置字段值转换规则
7. 已配置值转换规则的字段会显示🔄标记
8. 确认无误后点击"开始转换"

## 字段匹配规则

- **高置信度（绿色）**：字段名称完全一样（去掉 `*` 并去除首尾空格后比较）
- **中置信度（黄色）**：字段里包含相同含义的词语
- **手动映射（蓝色）**：用户手动建立的映射关系

## 文件说明

```
excel-data-transformer/
├── src/
│   ├── index.js          # 入口文件
│   ├── server.js         # Web 服务器
│   ├── fieldMapper.js    # 字段映射逻辑
│   ├── excelParser.js    # Excel 解析
│   ├── dataTransformer.js # 数据转换
│   ├── config/           # 配置管理
│   ├── utils/            # 工具函数
│   └── public/
│       ├── index.html    # 主页面
│       ├── js/
│       │   └── app.js    # 前端逻辑
│       └── css/
│           └── style.css # 样式文件
├── tests/                # 测试文件
├── transform_excel.js    # 主程序入口
├── package.json          # 项目配置
├── README.md             # 使用说明
├── CHANGELOG.md          # 更新日志
├── start.bat             # Windows 启动脚本
├── start.sh              # macOS/Linux 启动脚本
├── Dockerfile            # Docker 配置
├── docker-compose.yml    # Docker Compose 配置
└── ecosystem.config.js   # PM2 配置
```

## 版本历史

### v1.7.0 (2026-03-21)
#### 核心功能优化
- **数据转换逻辑重构**：修复字段映射数据填充逻辑，现在严格按照映射关系将源数据填充到目标列
- **空值处理优化**：源字段为空时，目标字段正确设置为空值，不再使用模板原始值填充
- **模板数据处理优化**：生成结果文件时只使用模板的表头结构，不复制模板的数据行内容
- **配置名称保存修复**：保存配置时正确保存配置名称字段，解决加载配置时显示"undefined"的问题
- **配置文件字段完善**：保存配置时为所有映射关系添加 `sourceField` 和 `targetField` 字段描述

#### 用户体验优化
- **保存配置提示优化**：保存配置前检查必填字段映射情况，如有未映射的必填字段先提示确认
- **配置列表自动刷新**：保存配置后自动刷新配置下拉列表并选中刚保存的配置
- **保存并转换功能修复**：修复点击"保存并转换"后配置保存了但没有执行转换的问题
- **输出文件名优化**：转换生成的文件名使用当前配置的名称作为前缀，不再显示"默认配置"
- **数据预览功能修复**：修复源文件数据预览打开后看不到数据的问题
- **字段映射自动建立**：选择目标文件后，如果没有匹配的配置，自动建立字段名相同的映射并提示新建配置

#### 问题修复
- **映射分析失败修复**：修复多处 `previewData.mappings` 未进行空值检查导致的报错
- **转换时参数错误修复**：修复 `buildOutputRows` 和 `writeOutputFile` 函数参数顺序错误
- **Workbook 无效错误修复**：修复 `writeOutputFile` 函数缺少返回值导致的"Invalid Workbook"错误
- **必填字段检测优化**：点击"开始转换"时重新计算必填字段映射情况，确保提示准确
- **配置加载空值检查**：添加配置加载时的空值检查，避免加载不存在的配置时报错

### v1.6.0 (2026-03-19)
- 连线交互优化：连线显示在面板上方，起点和终点不会被遮挡
- 操作提示更新：包含建立映射、删除映射和设置转换规则的完整说明
- 必填缺失统计修复：正确统计已映射的必填字段
- 服务持续运行：生成文件后服务不再自动关闭
- 连线点击区域修复：可以在连线任何位置点击删除
- 字段匹配逻辑优化：只有真正有匹配关系的字段才会建立映射

### v1.5.0 (2025-03-16)
- 数据预览弹出框：点击源文件行数可弹出预览窗口
- 配置变化检测：转换前自动检测未保存的更改
- Docker 和 PM2 部署支持

### v1.4.0 (2026-03-15)
- 界面布局优化：调整三个区域的比例
- 统计信息优化：显示字段数量和映射状态
- 逻辑运算功能：支持复杂逻辑表达式求值

### v1.3.0 (2026-03-14)
- 代码结构优化：模块化重构
- 错误处理优化：完善的错误处理机制

### v1.2.0 (2024-03-13)
- 新增字段值转换规则功能
- 点击连线中间的⚙按钮可配置值转换规则

### v1.1.0 (2024-03-13)
- 新增映射配置保存/加载功能
- 新增数据预览功能

### v1.0.0
- 初始版本
- 智能字段映射
- 可视化映射调整
- Excel 数据转换

## 常见问题

**Q: 为什么打开浏览器后显示空白？**
A: 请确保命令行窗口没有关闭，服务正在运行中。

**Q: 转换后的文件保存在哪里？**
A: 默认保存在源数据文件同目录下，文件名添加"_转换结果"后缀。也可以在界面上指定输出路径。

**Q: 支持哪些 Excel 格式？**
A: 支持 .xlsx 和 .xls 格式。

**Q: 如何删除映射关系？**
A: 点击连线即可删除该映射关系。

**Q: 如何设置字段值转换规则？**
A: 点击连线中间的⚙按钮，在弹出的窗口中添加转换规则。

## 技术支持

如有问题，请联系开发者或提交 Issue。

GitHub: https://github.com/li-xinyuan/excel-data-transformer
